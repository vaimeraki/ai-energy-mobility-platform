name: ai-em
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pyspark=3.5
  - pandas=2.1
  - pyarrow=13.0
  - requests=2.31
  - sqlalchemy=2.0
  - psycopg2=2.9
  - beautifulsoup4=4.12
  - lxml=4.9
  - jupyter=1.0
  - ipykernel=6.26
  - pip
  - pip:
    - delta-spark==3.0.0
    - tenacity==8.2.3
    - pydantic==2.5.3
    - python-dotenv==1.0.0
    - minio==7.2.0
    - dbt-core==1.7.4
    - dbt-postgres==1.7.4
    # apache-airflow: install separately in Phase 6 via requirements-orchestration.txt
    # (Airflow needs its own constraints file - see that file for why)
    - great-expectations==0.18.5
    - azure-storage-blob==12.19.0
