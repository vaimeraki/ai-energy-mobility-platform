# Core data engineering (Phases 1-4)
pandas==2.1.4
pyarrow==13.0.0
pyspark==3.5.0          # NOTE: was "apache-spark" (not a real pip package) - fixed
delta-spark==3.0.0
requests==2.31.0
tenacity==8.2.3          # retry/backoff for API ingestion
pydantic==2.5.3          # data contracts between pipeline stages
python-dotenv==1.0.0

# Local lakehouse client
minio==7.2.0

# Analytics & transformation
dbt-core==1.7.4
dbt-postgres==1.7.4      # local warehouse is Postgres, not BigQuery
SQLAlchemy==2.0.23
psycopg2-binary==2.9.9

# Data quality (Phase 4/6)
great-expectations==0.18.5

# Cloud (Azure only - matches infra/terraform)
azure-storage-blob==12.19.0

# French data sources
beautifulsoup4==4.12.2
lxml==4.9.3
